@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        var path = Navigation.ToBaseRelativePath(Navigation.Uri).Trim().Trim('/');
        if (string.IsNullOrEmpty(path))
            return;

        Navigation.NavigateTo("./");
    }
}
