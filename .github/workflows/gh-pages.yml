name: Deploy GitHub Pages

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Generate appsettings.json
        env:
          CUIDANET_USER: ${{ secrets.CUIDANET_USER }}
          CUIDANET_PASS: ${{ secrets.CUIDANET_PASS }}
          CUIDANET_LOGIN_URL: ${{ secrets.CUIDANET_LOGIN_URL }}
          CUIDANET_BASE_URL: ${{ secrets.CUIDANET_BASE_URL }}
          AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
        run: node scripts/generate-appsettings.js

      - name: Publish Blazor WASM
        run: |
          dotnet publish -c Release -o publish
          # GitHub Pages project site lives under /Cuidamed/
          sed -i 's|<base href="/" />|<base href="/Cuidamed/" />|' publish/wwwroot/index.html
          sed -i 's|const base = "/";|const base = "/Cuidamed/";|' publish/wwwroot/service-worker.js
          # SPA fallback for Blazor client-side routes
          cp publish/wwwroot/index.html publish/wwwroot/404.html

      - uses: actions/upload-pages-artifact@v3
        with:
          path: publish/wwwroot

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
